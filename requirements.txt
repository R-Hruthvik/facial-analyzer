# ============================================================================
# AI-Powered Facial Engagement Analyzer — requirements.txt
#
# Optimised for CPU-only environments (AWS EC2 t3.medium / t2.micro).
# Use `opencv-python-headless` (not the full GUI package) on servers.
#
# Pinning style: use loose upper-bounds so pip can resolve across platforms.
# ============================================================================

# ---- Core video & image processing ----------------------------------------
opencv-python-headless>=4.9,<4.14       # OpenCV (headless for servers)
# opencv-python>=4.9,<4.14              # ← Uncomment for local development with GUI windows

# ---- Facial landmark detection --------------------------------------------
mediapipe>=0.10.30                       # 468-point Face Mesh model

# ---- Web framework & API ---------------------------------------------------
fastapi>=0.110,<0.139                    # REST API framework
uvicorn[standard]>=0.29,<0.50            # ASGI server
python-multipart>=0.0.9                  # Required for FastAPI file uploads
httpx>=0.27,<0.29                        # HTTP client for testing

# ---- Interactive dashboard -------------------------------------------------
streamlit>=1.30,<1.59                    # Frontend dashboard framework
plotly>=5.18,<6.9                        # Real-time metric charts

# ---- Data & numerics -------------------------------------------------------
numpy>=1.26,<2.6                         # Array operations
pydantic>=2.5,<2.14                      # Data validation (FastAPI models)
pydantic-settings>=2.2,<2.8              # Settings management

# ---- Utilities -------------------------------------------------------------
python-dotenv>=1.0,<1.3                  # Environment variable loading

# ---- Testing ---------------------------------------------------------------
pytest>=8.0,<8.4                         # Test runner
